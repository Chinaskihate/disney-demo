export enum SearchCharacterNameActionTypes {
    FIND = "FIND",
    STOP = "STOP",
    CLEAR = "CLEAR"
}

export enum PageNumberActionTypes {
    NEXT = "NEXT",
    PREVIOUS = "PREVIOUS"
}

export enum ChangeSelectedCharacterActionTypes {
    SET = "SET"
}

export enum CommentActionTypes {
    COMMENT = "COMMENT"
}

export enum UserListActionTypes {
    ADD_CHARACTER = "ADD_CHARACTER",
    REMOVE_CHARACTER = "REMOVE_CHARACTER",
    ADD_LIST = "ADD_LIST",
    REMOVE_LIST = "REMOVE_LIST"
}

export enum SelectedListActionTypes {
    SELECT = "SELECT",
    CLEAR = "CLEAR"
}